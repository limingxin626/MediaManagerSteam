# 2026-04-03 开发日志

## 实现前后端双向同步架构

### 背景

之前的同步方案存在以下问题：
- 全量同步不可扩展（每次拉取全部数据）
- 后端没有 `POST /api/sync/apply` 端点（Android push 完全不工作）
- 缺少删除追踪（硬删除后客户端无感知）
- 无后台同步、无网络检测、无实时推送

### 实现内容

#### 后端

**新增 `SyncLog` 模型** (`backend/app/models/__init__.py`)
- 记录 `MESSAGE | ACTOR | MEDIA | TAG` 的 `UPSERT | DELETE` 操作
- 复合索引 `(timestamp, id)` + `(entity_type, entity_id)`
- 保留期 90 天

**新增 `sync_log_service.py`**
- SQLAlchemy `after_flush` 事件监听器：与业务数据在同一事务中原子写入
- 使用 thread-local 防止递归 flush
- 进程内 SSE 事件总线（`asyncio.Queue` 列表）
- `after_commit` 后广播通知给所有 SSE 订阅者

**新增 `routers/sync.py`** — 三个端点：
1. `GET /sync/changes?since={ISO}&limit=500`
   - 增量拉取，返回去重后的变更列表
   - UPSERT 附带完整实体快照（MESSAGE 含嵌套 media_items + tags）
   - since 为空/超过 90 天 → HTTP 410，客户端回退全量同步
2. `POST /api/sync/apply`
   - 按序处理 Android 推送的变更
   - Last-write-wins by `updated_at`，逐条 try/catch 不中断批次
   - camelCase → snake_case 字段映射
3. `GET /sync/events`
   - SSE 实时通知（仅携带元数据，不含完整数据）
   - 30 秒心跳保活

**修改 `app/__init__.py`**
- 启动时注册 SyncLog 监听器
- `Base.metadata.create_all(checkfirst=True)` 自动建表

#### Android

**新增依赖** (`build.gradle.kts`)
- `okhttp3:okhttp:4.12.0` + `okhttp3:okhttp-sse:4.12.0`（SSE）
- `androidx.work:work-runtime-ktx:2.9.1`（后台同步）

**扩展 `SyncService.kt`**
- 新增 `getChanges(since, limit)` 接口，返回 `Response<RemoteChangesResponse>`

**新增 `SyncModels`**
- `RemoteChangesResponse` / `RemoteChangeItem`
- `SyncResult.NeedFullSync`（410 响应时使用）

**`MessageRepository.syncIncremental(since)`**
- 增量拉取，处理 UPSERT（MESSAGE/ACTOR/MEDIA/TAG）和 DELETE
- has_more=true 时循环拉取
- 410 → 返回 `NeedFullSync`，调用方回退全量同步
- Gson 反序列化 `Map<String, Any?>` → 类型安全的 data class

**新建 `NetworkMonitor.kt`**
- `ConnectivityManager.NetworkCallback` 监听 WiFi 状态
- 暴露 `StateFlow<Boolean>` 供订阅

**新建 `SyncWorker.kt`**
- `CoroutineWorker`：push outbox → pull 增量（410 → 全量）
- `schedulePeriodicSync()`：每 15 分钟，WiFi constraint
- `scheduleImmediateSync()`：网络恢复时一次性触发
- `SharedPreferences` 存储 `last_sync_time`

**新建 `SseListener.kt`**
- OkHttp `EventSource` 连接 `/sync/events`
- 500ms debounce 触发增量同步
- 指数退避重连（2s → 30s）

**`MainActivity.kt`**
- 启动时调度定期同步
- 监听网络恢复触发即时同步

**`DatabaseManager.kt`**
- 注入 `NetworkMonitor`

#### Vue

**新建 `useSyncEvents.ts`**
- `EventSource` 连接 `/sync/events`
- 自动重连（指数退避）
- 回调接收 `(entityType, entityId, operation)`

### 文件变更清单

| 文件 | 类型 |
|------|------|
| `backend/app/models/__init__.py` | 修改 |
| `backend/app/services/sync_log_service.py` | 新建 |
| `backend/app/schemas/sync.py` | 新建 |
| `backend/app/routers/sync.py` | 新建 |
| `backend/app/routers/__init__.py` | 修改 |
| `backend/app/__init__.py` | 修改 |
| `android/app/build.gradle.kts` | 修改 |
| `android/.../service/SyncService.kt` | 修改 |
| `android/.../model/SyncModels.kt` | 修改 |
| `android/.../repository/MessageRepository.kt` | 修改 |
| `android/.../database/dao/TagDao.kt` | 修改 |
| `android/.../service/NetworkMonitor.kt` | 新建 |
| `android/.../service/SyncWorker.kt` | 新建 |
| `android/.../service/SseListener.kt` | 新建 |
| `android/.../data/DatabaseManager.kt` | 修改 |
| `android/.../MainActivity.kt` | 修改 |
| `vue/src/composables/useSyncEvents.ts` | 新建 |

---

## 后端 Bug 修复与性能改进

### 背景

对照 ARCHITECTURE.md 深度分析后端代码，发现多处 bug 和性能问题，统一修复。

### 修复内容

#### B-1 | sync/apply 批量原子性（严重）

- **文件**：`backend/app/routers/sync.py`
- **修复**：将 `POST /api/sync/apply` 的逐条 `db.commit()` 改为整批单一事务，全部成功才 commit，任意失败则全量 rollback。

#### B-3 | 大文件哈希碰撞（严重）

- **文件**：`backend/app/utils/__init__.py`
- **修复**：>100MB 文件改为采样首尾各 4MB 计算 blake2b（并前缀加文件大小），不再直接用文件大小作为 hash，消除大文件哈希碰撞导致误判重复的问题。

#### B-4 | 游标分页同 timestamp 跳行（高）

- **文件**：`backend/app/routers/sync.py`、`backend/app/schemas/sync.py`
- **修复**：`GET /sync/changes` 改为 `(timestamp, id)` 复合游标：
  - 查询条件：`(timestamp > t) OR (timestamp = t AND id > last_id)`
  - 新增 `since_id` 请求参数和 `next_cursor_id` 响应字段

#### B-6 | N+1 查询（高）

- **文件**：`backend/app/routers/actor.py`
- **修复**：演员列表页改用 `GROUP BY` 子查询批量获取 `message_count`；演员详情页同理批量获取各消息的 `media_count`，由 O(N) 降为 O(1)。

#### B-7 | 缺少复合索引（中）

- **文件**：`backend/app/models/__init__.py`
- **新增**：`Index("ix_mm_created_at_position", "created_at", "position")` — 支持媒体网格复合游标分页的高效过滤。

#### B-8 | SSE 订阅队列内存泄漏（中）

- **文件**：`backend/app/services/sync_log_service.py`
- **修复**：
  - `_subscribers` 列表操作加 `threading.Lock` 保护
  - `asyncio.Queue` 加 `maxsize=100`
  - `_broadcast` 中 `put_nowait` 失败（队列满）时自动将孤立 queue 从订阅列表清除

#### B-9 | 数据完整性约束缺失（中）

- **文件**：`backend/app/models/__init__.py`
- **新增约束**：
  - `Tag.name` — `UniqueConstraint("uq_tag_name")`
  - `Actor.name` — `UniqueConstraint("uq_actor_name")`
  - `MessageMedia(message_id, position)` — `UniqueConstraint("uq_mm_message_position")`

#### B-13 | ffmpeg 路径默认值无警告（低）

- **文件**：`backend/app/config.py`、`backend/app/__init__.py`
- **修复**：新增 `AppConfig.check_paths()` 方法，启动时检查 `FFMPEG_PATH`/`FFPROBE_PATH` 是否存在，不存在则打 warning 并告知用户通过环境变量设置。

### 文件变更清单

| 文件 | 变更 |
|------|------|
| `backend/app/routers/sync.py` | B-1（批量原子事务）、B-4（复合游标） |
| `backend/app/schemas/sync.py` | B-4（新增 `next_cursor_id` 字段） |
| `backend/app/utils/__init__.py` | B-3（采样哈希） |
| `backend/app/routers/actor.py` | B-6（消灭 N+1） |
| `backend/app/models/__init__.py` | B-7（复合索引）、B-9（唯一约束） |
| `backend/app/services/sync_log_service.py` | B-8（SSE 队列保护） |
| `backend/app/config.py` | B-13（ffmpeg 路径 warning） |
| `backend/app/__init__.py` | B-13（启动时调用 check_paths） |

---

## 修复 STATUS.md #3 和 #4

### #3 sync/changes 复合游标 — 后端确认已完成

后端 `/sync/changes` 的 `since_id` 参数和 `next_cursor_id` 响应字段已在上次迭代中实现，本次确认后端侧无遗留工作。STATUS.md 更新说明后端已完成，剩余工作在 Android 端。

### #4 标签合并模式（B-5）— 已修复

- **文件**：`backend/app/services/message_service.py`、`backend/app/routers/sync.py`
- **修复**：`sync_tags_from_text()` 新增 `merge: bool = False` 参数
  - `merge=False`（默认）：全量替换，保持 message router 原有语义
  - `merge=True`：合并模式，保留现有标签，仅追加文本中新出现的标签
  - `sync.py` 中两处调用改为 `merge=True`，避免 Android UPSERT 时删除手动添加的标签

---

## 引入 Alembic 迁移管理（B-12）

### 背景

之前靠 `create_all(checkfirst=True)` 管理 schema，无版本追踪，无法追踪数据库变更历史。

### 实现内容

**初始化 Alembic**
- 安装 `alembic` 依赖
- 运行 `alembic init alembic` 初始化迁移目录结构

**配置 Alembic**
- `alembic.ini`：配置数据库 URL `sqlite:///./db_new.sqlite3`
- `alembic.ini`：设置带时间戳的迁移文件名格式
- `alembic/env.py`：导入 `app.models.Base` 作为 `target_metadata`

**创建 Baseline 迁移**
- 创建空迁移 `20260403_2150_2e17e07d0ec6_baseline.py`
- 使用 `alembic stamp head` 标记现有数据库已处于此版本

**移除 create_all**
- 从 `app/__init__.py` 移除 `Base.metadata.create_all(bind=engine, checkfirst=True)`
- 后续所有 schema 变更通过 Alembic 迁移脚本管理

### 文件变更清单

| 文件 | 变更 |
|------|------|
| `backend/pyproject.toml` | 添加 `alembic` 依赖 |
| `backend/alembic.ini` | 新建 |
| `backend/alembic/env.py` | 新建并配置 |
| `backend/alembic/versions/20260403_2150_2e17e07d0ec6_baseline.py` | 新建 |
| `backend/app/__init__.py` | 移除 `create_all` |

---

## 重算大文件 Hash（>100MB 文件使用文件大小作为 hash）

### 背景

之前 >100MB 文件的 `file_hash` 存的是文件大小字符串（如 `"524288000"`），现在改为采样哈希。现有记录的 hash 与新算法不兼容，会导致重复上传时无法命中去重。

### 实现内容

**创建重算脚本**
- `scripts/recalculate_hashes.py`：遍历所有 `file_hash` 为纯数字且等于 `file_size` 的记录
- 使用 `calculate_file_hash()` 重新计算采样哈希
- 检测新 hash 是否与现有记录冲突（避免错误合并）
- 每 10 条提交一次

**执行结果**
- 总计：582 条记录
- 成功更新：582 条
- 跳过：0 条
- 失败：0 条

### 文件变更清单

| 文件 | 变更 |
|------|------|
| `backend/scripts/recalculate_hashes.py` | 新建 |

---

## 修改 Duration 为毫秒并重新获取

### 背景

duration 原以秒为单位存储，改为毫秒以提高精度。

### 实现内容

**修改获取逻辑**
- `app/utils/__init__.py`：`get_media_info()` 中视频 duration 改为 `round(float(format_data['duration']) * 1000)`
- `app/utils/__init__.py`：GIF duration fallback 改为直接使用毫秒值（不再除以 1000）

**创建重算脚本**
- `scripts/recalculate_durations.py`：遍历所有媒体记录，重新获取 duration
- 使用 `MediaInfoUtils.get_media_info()` 获取正确的 duration（毫秒）

**执行结果**
- 总计：9826 条记录
- 成功更新：4731 条（视频/GIF）
- 跳过：0 条
- 失败：5095 条（静态图片无 duration，正常）

### 文件变更清单

| 文件 | 变更 |
|------|------|
| `backend/app/utils/__init__.py` | 修改 duration 获取逻辑 |
| `backend/scripts/recalculate_durations.py` | 新建 |

---

## Duration 字段重命名为 duration_ms

### 背景

为明确单位，将 `duration` 字段重命名为 `duration_ms`。

### 实现内容

**创建迁移脚本**
- `alembic/versions/20260403_2207_0388688167b2_rename_duration_to_duration_ms.py`
- `upgrade()`：`ALTER TABLE media RENAME COLUMN duration TO duration_ms`
- `downgrade()`：`ALTER TABLE media RENAME COLUMN duration_ms TO duration`

**更新模型定义**
- `app/models/__init__.py`：`duration = Column(Integer, nullable=True)` → `duration_ms = Column(Integer, nullable=True)`

**更新 Schema**
- `app/schemas/media.py`：`duration` → `duration_ms`
- `app/schemas/message.py`：`MessageMediaItem` 和 `MessageSyncMediaItem` 的 `duration` → `duration_ms`

**更新 Routers**
- `app/routers/message.py`：`r.media.duration` → `r.media.duration_ms`（2 处）
- `app/routers/sync.py`：`m.duration` → `m.duration_ms`、`media.duration` → `media.duration_ms`（2 处）
- `app/routers/media.py`：`item.duration` → `item.duration_ms`、`media.duration` → `media.duration_ms`（4 处）

**执行迁移**
- 迁移成功执行，数据库字段已重命名

### 文件变更清单

| 文件 | 变更 |
|------|------|
| `backend/alembic/versions/20260403_2207_0388688167b2_rename_duration_to_duration_ms.py` | 新建 |
| `backend/app/models/__init__.py` | 字段重命名 |
| `backend/app/schemas/media.py` | 字段重命名 |
| `backend/app/schemas/message.py` | 字段重命名 |
| `backend/app/routers/message.py` | 字段引用更新 |
| `backend/app/routers/sync.py` | 字段引用更新 |
| `backend/app/routers/media.py` | 字段引用更新 |

---

## 简化 Pydantic 赋值逻辑

### 背景

Schema 已设置 `from_attributes = True`，可以使用 `model_validate()` 自动从对象提取值，无需手动逐个赋值。

### 实现内容

**添加 from_attributes 配置**
- `app/schemas/message.py`：`MessageMediaItem` 和 `MessageSyncMediaItem` 添加 `Config` 类
- `app/schemas/media.py`：`MediaDetailResponse` 添加 `Config` 类

**添加 datetime 转换 validator**
- `app/schemas/media.py`：添加 `@field_validator('created_at', 'updated_at', mode='before')` 将 `datetime` 转换为 `str`
- `app/schemas/message.py`：`MessageResponse` 和 `MessageSyncResponse` 添加相同的 validator

**简化赋值逻辑**
- `app/routers/message.py`：`_media_items_for()` 改为 `[MessageMediaItem.model_validate(r.media) for r in relations]`
- `app/routers/message.py`：`_message_snapshot()` 改为 `[MessageSyncMediaItem.model_validate(r.media, position=r.position) for r in relations]`
- `app/routers/media.py`：4 处手动赋值改为 `[MediaResponse.model_validate(item) for item in items]`
- `app/routers/media.py`：`get_media_detail()` 改为 `MediaDetailResponse.model_validate(media, messages=messages)`

### 文件变更清单

| 文件 | 变更 |
|------|------|
| `backend/app/schemas/media.py` | 添加 Config、添加 datetime validator |
| `backend/app/schemas/message.py` | 添加 Config、添加 datetime validator |
| `backend/app/routers/message.py` | 简化赋值逻辑 |
| `backend/app/routers/media.py` | 简化赋值逻辑 |
