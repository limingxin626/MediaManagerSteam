## 1. 拼音首字母工具

- [x] 1.1 创建 `vue/src/utils/pinyinInitial.ts`，包含汉字→拼音首字母映射表和转换函数 `getPinyinInitials(text: string): string`（中文字符取首字母，英文/数字保留原样）

## 2. 补全逻辑改造

- [x] 2.1 在 `useTagAutocomplete.ts` 的 `onInput` 过滤逻辑中集成拼音首字母匹配：纯文本匹配和拼音前缀匹配并行，去重后合并结果
- [x] 2.2 调整排序：精确文本匹配的标签排在拼音匹配的标签前面，各组内部保留最近使用排序

## 3. 验证

- [ ] 3.1 手动测试（需用户验证）：输入 `#sj` 能匹配「数据」类标签，输入中文仍正常工作，中英混合标签可匹配
