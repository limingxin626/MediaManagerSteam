## 1. Header 统一

- [ ] 1.1 Message.vue：header 标题改为 `text-lg font-bold`，间距改为 `py-3`
- [ ] 1.2 Media.vue：header 标题改为 `text-lg font-bold`，间距保持 `py-3`
- [ ] 1.3 Actor.vue：header 标题改为 `text-lg font-bold`，间距改为 `py-3`

## 2. 筛选按钮颜色统一

- [ ] 2.1 Media.vue：将激活态筛选按钮从 `bg-pink-600` 改为 `bg-[var(--color-primary-600)]`

## 3. 空状态统一

- [ ] 3.1 对比三个页面的空状态布局，统一为居中 flex + 图标 + 文字的一致样式

## 4. 验证

- [ ] 4.1 启动前端 dev server，逐页检查 Message / Media / Actor 页面的视觉一致性
