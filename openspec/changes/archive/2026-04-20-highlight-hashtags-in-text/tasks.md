## 1. CSS 样式

- [x] 1.1 在 `vue/src/style.css` 中添加 `.hashtag-inline` 样式（亮色 + 暗色模式）

## 2. 文本渲染

- [x] 2.1 在 `MessageCard.vue` 的 `renderedText` computed 中，对 marked 输出做 hashtag 正则替换，包裹为 `<span class="hashtag-inline">`
